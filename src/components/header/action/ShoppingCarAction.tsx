import { ShoppingCartIcon } from "lucide-react";

type Props = {};

const ShoppingCarAction = (props: Props) => {
  return (
    <div>
      <ShoppingCartIcon />
    </div>
  );
};

export default ShoppingCarAction;
